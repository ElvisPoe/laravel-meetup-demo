<?php

// use App\Models\Task;

// it('should show the in progress label for a task on mobile', function () {
//     // Arrange
//     $task = Task::factory()->inProgress()->create([
//         'title' => 'Ship slides',
//     ]);

//     // Act
//     $page = $this->visit('/')->on()->mobile();

//     // Assert
//     $page->assertSee($task->title)
//         ->assertSee('In progress')
//         ->assertNoJavaScriptErrors();
// });
